# PHPMailer

Place the official PHPMailer source files here:

```text
src/Exception.php
src/PHPMailer.php
src/SMTP.php
```

The API loads these files directly and does not require Composer.

Use the official PHPMailer release from:
https://github.com/PHPMailer/PHPMailer/releases
